This is a generated static portfolio for Fawzy based on the provided `Fawzy.pdf`.

How to preview locally:

1. Open `site/index.html` in a browser (double-click or run a local static server).

Optional: Serve with a simple Python HTTP server:

```powershell
# from workspace root (d:/Freelancing/Portfolio/Fawzy)
python -m http.server 8000
# then open http://localhost:8000/site/
```

To publish: push to a GitHub repo and enable GitHub Pages, or deploy `site/` to Vercel/Netlify.
